# Base URL of the Aetheris backend API. Public by design: the browser calls it
# directly. It must never carry a credential -- exchange keys stay server-side,
# and this build has none at all.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
